# 📍 项目地图

## 🗺️ 文件导航

```
shopo-alpha-mvp/
│
├── 📖 入口文档
│   ├── START-HERE.md ⭐          ← 从这里开始！
│   ├── FINAL-SUMMARY.md          ← 完整总结
│   └── PROJECT-MAP.md            ← 你在这里
│
├── 📚 学习文档
│   ├── QUICKSTART.md             ← 5 分钟快速上手
│   ├── PROJECT-SUMMARY.md        ← 项目架构和设计
│   ├── MVP-PLAN.md               ← 3 周开发计划
│   └── DEPLOYMENT.md             ← 部署和运维指南
│
├── ⚙️ 配置文件
│   ├── .env.example              ← 环境变量模板
│   ├── package.json              ← 依赖和脚本
│   ├── tsconfig.json             ← TypeScript 配置
│   ├── tailwind.config.ts        ← Tailwind 配置
│   ├── next.config.js            ← Next.js 配置
│   └── .gitignore                ← Git 忽略规则
│
├── 💻 前端代码
│   └── app/
│       ├── layout.tsx            ← 全局布局
│       ├── page.tsx              ← 首页
│       ├── globals.css           ← 全局样式
│       └── agents/
│           └── [slug]/
│               └── page.tsx      ← Agent 详情页
│
├── 🔧 工具库
│   └── lib/
│       ├── supabase.ts           ← Supabase 客户端和类型
│       └── openai.ts             ← OpenAI 集成和 AI 分析
│
├── 🕷️ 爬虫系统
│   └── crawler/
│       ├── run.ts                ← 爬虫入口（运行这个）
│       ├── enricher.ts           ← AI 分析和数据增强
│       └── sources/
│           └── gpt-store.ts      ← GPT Store 爬虫
│
├── 🗄️ 数据库
│   └── supabase/
│       ├── schema.sql            ← 数据库结构（在 Supabase 执行）
│       └── seed.sql              ← 初始数据（在 Supabase 执行）
│
└── 🛠️ 脚本工具
    └── scripts/
        └── setup-database.js     ← 数据库初始化脚本
```

## 🎯 快速查找

### 我想...

**开始项目**
→ `START-HERE.md`

**快速上手**
→ `QUICKSTART.md`

**了解架构**
→ `PROJECT-SUMMARY.md`

**查看计划**
→ `MVP-PLAN.md`

**部署上线**
→ `DEPLOYMENT.md`

**修改首页**
→ `app/page.tsx`

**修改详情页**
→ `app/agents/[slug]/page.tsx`

**修改爬虫**
→ `crawler/sources/gpt-store.ts`

**修改 AI 分析**
→ `lib/openai.ts`

**修改数据库**
→ `supabase/schema.sql`

**配置环境**
→ `.env.example`

**添加依赖**
→ `package.json`

## 🔄 工作流程

### 1. 开发流程

```
1. 修改代码
   ↓
2. npm run dev（查看效果）
   ↓
3. 测试功能
   ↓
4. 提交代码
   ↓
5. 部署（Vercel 自动）
```

### 2. 爬虫流程

```
1. 修改 crawler/sources/gpt-store.ts
   ↓
2. npm run crawler（运行爬虫）
   ↓
3. 查看 Supabase（验证数据）
   ↓
4. 刷新网站（查看新 Agents）
```

### 3. 数据库修改流程

```
1. 修改 supabase/schema.sql
   ↓
2. 在 Supabase Dashboard 执行 SQL
   ↓
3. 更新 lib/supabase.ts 的类型定义
   ↓
4. 重启开发服务器
```

## 📊 文件重要性

### ⭐⭐⭐ 核心文件（必须理解）

- `app/page.tsx` - 首页
- `app/agents/[slug]/page.tsx` - 详情页
- `crawler/run.ts` - 爬虫入口
- `lib/openai.ts` - AI 分析
- `supabase/schema.sql` - 数据库结构

### ⭐⭐ 重要文件（需要了解）

- `lib/supabase.ts` - 数据库客户端
- `crawler/enricher.ts` - 数据处理
- `crawler/sources/gpt-store.ts` - 爬虫实现
- `package.json` - 依赖管理

### ⭐ 配置文件（按需修改）

- `.env.example` - 环境变量
- `next.config.js` - Next.js 配置
- `tailwind.config.ts` - 样式配置
- `tsconfig.json` - TypeScript 配置

## 🎨 代码组织

### 前端（app/）

```
app/
├── layout.tsx          # 导航栏、页脚
├── page.tsx            # 首页：统计、分类、最新 Agents
└── agents/
    └── [slug]/
        └── page.tsx    # 详情页：完整信息、对比、FAQ
```

### 后端（lib/）

```
lib/
├── supabase.ts         # 数据库操作、类型定义
└── openai.ts           # AI 分析、对比生成
```

### 爬虫（crawler/）

```
crawler/
├── run.ts              # 主流程：爬取 → 分析 → 保存
├── enricher.ts         # AI 分析和数据增强
└── sources/
    └── gpt-store.ts    # 具体爬虫实现
```

## 🔍 代码搜索

### 查找功能

**搜索关键词：**
- `supabaseAdmin` - 数据库操作
- `openai.chat` - AI 调用
- `crawl` - 爬虫相关
- `Schema.org` - SEO 优化
- `metadata` - 页面元数据

### 常见任务

**添加新页面：**
1. 在 `app/` 下创建文件夹
2. 添加 `page.tsx`
3. 导出 React 组件

**添加新爬虫源：**
1. 在 `crawler/sources/` 创建文件
2. 实现 `crawl` 函数
3. 在 `crawler/run.ts` 中调用

**修改 AI 分析：**
1. 编辑 `lib/openai.ts`
2. 修改 `analyzeAgent` 函数的 prompt
3. 重新运行爬虫

**修改数据库：**
1. 编辑 `supabase/schema.sql`
2. 在 Supabase Dashboard 执行
3. 更新 `lib/supabase.ts` 类型

## 📝 开发检查清单

### 首次设置
- [ ] 阅读 `START-HERE.md`
- [ ] 安装依赖 `npm install`
- [ ] 配置 `.env.local`
- [ ] 初始化数据库
- [ ] 运行 `npm run dev`

### 开发中
- [ ] 代码有注释
- [ ] 类型定义完整
- [ ] 错误处理到位
- [ ] 测试功能正常

### 部署前
- [ ] 环境变量配置
- [ ] 数据库迁移完成
- [ ] 爬虫测试通过
- [ ] 页面加载正常
- [ ] SEO 优化完成

## 🚀 快速命令

```bash
# 开发
npm run dev              # 启动开发服务器
npm run build            # 构建生产版本
npm run start            # 启动生产服务器

# 爬虫
npm run crawler          # 运行爬虫

# 数据库
npm run db:setup         # 初始化数据库（需要手动执行 SQL）

# 部署
vercel                   # 部署到 Vercel
vercel --prod            # 生产部署
```

## 💡 提示

### 开发技巧
- 使用 `console.log` 调试
- 查看 Supabase Dashboard 验证数据
- 使用 Vercel Preview 测试部署

### 常见问题
- 爬虫失败 → 使用种子数据
- 数据库错误 → 检查 RLS 策略
- 页面不更新 → 清除 `.next` 缓存

### 性能优化
- 使用 SSG 而不是 SSR
- 启用 ISR（已配置）
- 优化图片（Next.js Image）
- 减少 API 调用

## 🎯 下一步

1. **今天**：阅读 `START-HERE.md`，配置环境
2. **本周**：完成核心功能，本地测试
3. **下周**：部署上线，开始推广

---

**记住：这个地图会帮你快速找到需要的文件！** 📍

需要帮助？查看 `START-HERE.md` 🚀
